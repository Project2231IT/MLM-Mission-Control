# 2026-03-25

## Playback Fixes
- Root cause: TVT/onvif NVRs (Dock17 VP, MLM Cafe) output H.265/HEVC — Chrome can't play in MP4 container
- Tried `#video=h264` transcode — crushed the VM at 146% CPU (ffmpeg real-time transcoding 1080p)
- Switched to WebRTC player (go2rtc serves H.265 natively via WebRTC, Chrome 107+ supports it)
- Restored `#video=copy` passthrough — no CPU cost
- Added TVT playback URL support: date/time/timelen RTSP params for recorded video
- NVRs serve at 1x speed — no fast-forward possible via RTSP
- Added stream readiness polling (wait for ffmpeg to connect before returning mp4 URL)
- Slider now supports second-precision at fine zoom levels (5m/15m/30m)
- Motion marks feature: built but disabled at Jake's request (too resource intensive)

## Signage Control Fixes
- Xibo schedule API requires `campaignId` not `layoutId` — rewrote from swagger spec
- `fromDt` must be `Y-m-d H:i:s` format (not '0' or unix timestamp)
- `toDt` is optional — don't send for "runs forever"
- Campaign POST requires `type: 'list'` in Xibo 4.x
- `displayOrder` and `isPriority` are REQUIRED integers
- New display found: "Jakes Office TEST" (ID 24)

## Signage Control — Major Features Added
- Login system: JWT auth, middleware, `/login` page, 24h tokens
- Default admin: `it@project2231.com` / `P2231signage!`
- User admin page (`/admin`): create users, reset passwords, delete users, role-based (admin/editor/viewer)
- Schedule Manager (`/calendar`): view all scheduled events, filter by display, remove events
- "When" step redesigned: Right Now / Date Range / Recurring / Always
- Recurring: weekday/weekend presets, individual day toggles, time windows, optional date bounds
- Xibo API: `campaignId` required (not `layoutId`), `dayPartId=0` with explicit dates, `type: 'list'` for campaigns
- Full access scope enabled on Xibo API client
- Users stored in `data/users.json` on signage-control server

## Signage Control — Auth & Admin
- Login system deployed: JWT tokens, 24h expiry, middleware protects all routes
- Default admin: it@project2231.com / P2231signage!
- Second user added by Jake: grant@hometownprofit.com (admin)
- Admin page at /admin: create users, reset passwords, delete users
- Calendar/schedule page at /calendar: view all events by display, remove events
- Profile widget: bottom-left avatar, shows name/email/role, links to schedule/admin/logout
- Users stored in data/users.json, passwords hashed with PBKDF2
- Roles: admin (full), editor (publish+remove), viewer (read-only)
- Xibo API client now has full account access (Jake enabled it)

## Signage Control — When Step Redesign
- 4 scheduling modes: Right Now, Date Range, Recurring, Always
- Recurring: weekday/weekend/custom day toggles, time range, optional date bounds
- Uses Xibo recurrenceType=Week, recurrenceRepeatsOn for day filtering
- Dates now in Y-m-d H:i:s format (Xibo requirement)

## Signage Control — Auth & Admin
- Login system added: JWT + cookie, middleware protects all routes
- Default admin: it@project2231.com / P2231signage!
- Users stored in data/users.json (pbkdf2 hashed passwords)
- Admin page at /admin: create users, reset passwords, delete users
- Roles: admin (full), editor (publish), viewer (read-only)
- Profile menu: bottom-left avatar button → Schedule Manager, Administration, Sign Out
- Calendar/Schedule Manager at /calendar: view events by display (dropdown filter), remove events
- Grant Girsky (grant@hometownprofit.com) added as admin by Jake

## Signage Control — Scheduling Fixes (from Xibo swagger spec)
- Xibo requires campaignId not layoutId for POST /schedule
- fromDt must be Y-m-d H:i:s format, toDt optional
- dayPartId=1 (always) is broken in Xibo, use dayPartId=0 with far-future toDt
- Campaign POST requires type: 'list' in Xibo 4.x
- API client needed "Full account access" scope for campaign creation
- Added recurring schedule support: weekdays/weekends/custom days via recurrenceType=Week + recurrenceRepeatsOn

## Signage Control — Major Updates
- **Login system**: JWT auth, middleware protects all routes, cookie-based sessions (24h)
  - Default admin: it@project2231.com / P2231signage!
  - Users stored in data/users.json with PBKDF2 password hashing
- **Admin page** (/admin): create users, reset passwords, delete users (admin-only)
- **Profile widget**: bottom-left avatar button with Schedule Manager, Administration, Sign Out
- **Calendar/Schedule Manager** (/calendar): view all scheduled events, filter by display dropdown, remove events
  - Fixed Xibo pagination: now fetches up to 1000 events (was defaulting to 10)
- **When step redesigned**: 4 options — Right Now, Date Range, Recurring (weekdays/weekends/custom days), Always
  - Recurring uses Xibo native recurrenceType=Week with recurrenceRepeatsOn
- **Thumbnails**: layout previews on calendar events and layout picker grid
  - Proxy via /api/thumbnail, fetches first media widget from layout, cached 1hr
- **Xibo API fixes**: campaignId (not layoutId), dayPartId=0 with explicit dates, proper Y-m-d H:i:s format
- Jake added user: Grant Girsky (grant@hometownprofit.com, admin)
- New display: "Jakes Office TEST" (displayId=24, displayGroupId=25)

## Signage Control — New Features
- **Login system**: JWT auth, middleware protection, users stored in data/users.json
  - Default admin: it@project2231.com / P2231signage!
  - Roles: admin/editor/viewer
  - Grant Girsky also added as admin (grant@hometownprofit.com)
- **Admin page** (/admin): create users, reset passwords, delete users (admin-only)
- **Profile menu**: bottom-left avatar button with logout, schedule manager, admin links
- **Calendar/Schedule Manager** (/calendar): view all Xibo events, filter by display (dropdown), remove events
  - Fixed pagination: fetches up to 1000 events (was defaulting to 10)
- **When step redesign**: 4 modes — Right Now, Date Range, Recurring (weekdays/weekends/custom days), Always
  - Recurring uses Xibo native recurrenceType=Week with recurrenceRepeatsOn
- **Thumbnails**: layout cards and calendar events show preview images from Xibo library
  - Searches library by layout name to find matching media
  - Falls back to transparent pixel if no match
- **Xibo API fixes** (from swagger spec):
  - `campaignId` not `layoutId` for schedule POST
  - `fromDt` in Y-m-d H:i:s format, `toDt` always required with dayPartId=0
  - Campaign POST requires type='list'
  - Full account access scope needed for campaign creation

## Camera Map
- Duplicate groups removed: 53→26 (removed auto-generated dupes)
- Set Dock 17 icon to 🍺 (field is `icon` not `emoji`)

## Signage Control — Major Features Added
- **Login/Auth**: JWT-based, middleware protects all routes, users stored in data/users.json
  - Default admin: it@project2231.com / P2231signage!
  - Roles: admin, editor, viewer
- **User Administration** (/admin): create users, reset passwords, delete users (admin only)
- **Profile menu**: bottom-left avatar, shows Schedule Manager, Administration (admin), Sign Out
- **Calendar/Schedule Manager** (/calendar): monthly calendar grid, day detail panel, remove events, display dropdown filter
- **Recurring schedules**: Weekdays/Weekends/custom days with time windows, uses Xibo recurrenceType=Week
- **Thumbnails**: layout picker + calendar events show Xibo library image thumbnails
- **Display maps**: now loops all zones (not just TCF), added TCF LED Wall zone with photo background
- **Screenshot fix**: changed object-cover to object-contain so full screenshot shows

## Checklist Fix
- Camera IDs were wrong after migration: cam-dock17-chN → cam-nvr-1773072476322-chN
- Old snapshot paths referenced Thor (/home/jake/.openclaw/workspace/argus/)
- Fixed and restarted checklist-capture; should resume capturing tonight

## Xibo API Lessons
- Schedule POST requires campaignId (not layoutId), resolved via layout API lookup
- fromDt REQUIRED in Y-m-d H:i:s format, toDt optional
- dayPartId=1 (always) is broken — use dayPartId=0 with far-future toDt
- Campaign POST requires type='list' in v4
- API client needed "Full account access" scope for campaign creation
- Pagination: default returns only 10 results, use start=0&length=1000

## Signage Control — New Features
- Login/auth system: JWT tokens, middleware protecting all routes, users.json storage
- Default admin: it@project2231.com / P2231signage! (Jake's email)
- Admin page (/admin): create users, reset passwords, delete users, role badges
- Profile widget: bottom-left avatar, popup menu with Schedule/Admin/Sign Out
- Calendar view (/calendar): monthly grid, event pills, day detail panel, remove events, display filter dropdown
- Thumbnails: layout picker + calendar events show Xibo library image thumbnails
- Display backgrounds: TCFLEDWALL has photo background in "See What's Playing" view
- Recurring schedules: Weekdays/Weekends/custom days, time windows, Xibo recurrenceType=Week
- Grant Girsky added as admin user (grant@hometownprofit.com)

## Checklist Fix
- Camera IDs were wrong after migration: cam-dock17-chN → cam-nvr-1773072476322-chN
- No snapshots since March 22 because of this
- Fixed and restarted checklist-capture PM2 service
- Video tour for camera mapping postponed to tomorrow (event in TCF today)
- Video tour postponed to tomorrow (event at TCF today)
- Video tour postponed to tomorrow (event at TCF today)

## Signage Control — New Features
- Login system: JWT auth, middleware protecting all routes, `it@project2231.com` admin account
- User administration page (`/admin`): create users, reset passwords, delete users (admin-only)
- Schedule Manager page (`/calendar`): view all scheduled events, filter by display dropdown, remove events
- Profile menu: bottom-left avatar, shows Schedule/Administration/Sign Out
- "When" step redesigned: Right Now / Date Range / Recurring (weekdays/weekends/custom days) / Always
- Recurring uses Xibo native `recurrenceType=Week` + `recurrenceRepeatsOn`
- Thumbnails: searches Xibo media library by layout name for preview images
- Second user created: Grant Girsky (grant@hometownprofit.com, admin)

## Xibo API Lessons
- Schedule POST requires `campaignId` not `layoutId` (layouts have auto-campaigns)
- `fromDt` must be `Y-m-d H:i:s` format, REQUIRED
- `dayPartId=1` (always) is broken — use `dayPartId=0` with far-future toDt
- Campaign POST requires `type: 'list'` in v4
- Pagination: default page size is ~10, must pass `start=0&length=1000`
- Widget embed via layout API doesn't work in v4 — use library search instead
- API client needed "Full account access" scope for campaign operations

# 2026-03-26 — Argus Overhaul Day

## Checklist Email Fix
- Checklists weren't sending emails — analyze.js had no cron + no gateway access after move from Thor to Argus VM
- Added `--email-only` flag to analyze.js (skips vision, just builds report + sends email)
- Moved dotenv loading to top of file (was only in sendReportEmail)
- Added GATEWAY_URL/GATEWAY_TOKEN env vars to .env.local (but gateway API isn't OpenAI-compatible)
- Changed gateway bind from loopback → lan, added UFW rule for 18789
- Created OpenClaw cron job: daily 6AM Central, isolated agent SSHes into Argus, pulls snapshots, analyzes with vision, updates results, triggers --email-only for email
- Tested successfully: 2026-03-25 arcade games task → PASS ✅

## Barista Monitor v2
- Rebuilt from cup-tracking (broken, cups=0 always) to person-based dwell detection
- YOLOv8n tracks people → pickup zone dwell (10-30s) with barista present = handoff
- Cup detection kept as secondary fallback signal
- Cross-method suppression prevents double counting
- Pickup zone auto-derivation was too broad — manually set but still needs tuning
- Dwell min bumped from 2s → 10s per Jake's request
- Removed barista breakdown panel from dashboard BaristaMonitor.tsx
- Rebuilt Argus Next.js app and restarted

## People Counter v2
- Rebuilt from Opus vision LLM snapshots (broken, no gateway) to YOLO line-crossing
- YOLOv8n person tracking + line crossing detection on 5 RTSP cameras
- Round-robin processing, ~10 frames/30s per camera
- Configurable per-camera counting lines in people-counter-config.json
- Lines default to horizontal y=0.5 — need fine-tuning later
- Confirmed working: first entry detected on Front Door 2
- Cleared all historical people count data for fresh start
- PM2 config saved

## GPU Migration
- Argus VM has no GPU (Dell R740xd, Xeon E5-2698 v4, 12 vCPUs)
- Running YOLO on CPU caused load avg 20+ — unusable
- Migrated both YOLO processes to jakesopenclaw (RTX 4060)
- Installed torch+cu121, ultralytics, opencv on host
- Scripts in workspace/argus-ai/ with remote_sync.py for SSH-based data sync back to Argus
- GPU: 2% utilization, 596MB VRAM, 36°C — barely working
- Argus VM load dropped from 20 → 1.8

## Dashboard: Pickup Zone Drawing
- Added 3-step zone drawing to BaristaMonitor.tsx (barista → customer → pickup)
- Pickup zone shown in blue (#00d4ff)
- Updated API route to persist pickupZone in config.json
- Jake can now draw all three zones from the dashboard

## People Counter Line Tuning
- Pulled frames from all 5 cameras via vision analysis
- Set proper counting lines across actual doorways (not default y=0.5)
- False crossings dropped from 1,596 to reasonable numbers
- Lines may need further tuning — Jake is monitoring

## PM2 Services

### jakesopenclaw (GPU host)
- barista-monitor (v2, Python, YOLO on RTX 4060)
- people-counter (v2, Python, YOLO on RTX 4060)

### Argus VM (172.16.201.23)
- argus (Next.js dashboard, port 4000)
- barista-monitor — STOPPED (moved to GPU host)
- checklist-capture (Node.js, snapshots every 30s)
- go2rtc (stream proxy)
- people-counter — STOPPED (moved to GPU host)
- signage-control (Node.js)
- snapshot-cache (Node.js)
